@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

<nav class="navbar container" role="navigation" aria-label="main navigation">
    <div class="navbar-brand">
        <a class="navbar-item" asp-page="/Index">
            <strong class="is-size-4">Animal Rescue League</strong>
        </a>
        <a role="button" class="navbar-burger burger" aria-label="menu" aria-expanded="false" data-target="navbarBasicExample">
            <span aria-hidden="true"></span>
            <span aria-hidden="true"></span>
            <span aria-hidden="true"></span>
        </a>
    </div>
    <div id="navbar" class="navbar-menu">
        <div class="navbar-start">
            <a asp-page="/Index" class="navbar-item">Home</a>
            <a asp-page="/About" class="navbar-item">About</a>
            @if (User.IsInRole("Admin"))
            {
                <a asp-page="/Dashboard" class="navbar-item">User Dashboard</a>
            }
        </div>
        <div class="navbar-end">
            <div class="navbar-item">
                <div class="buttons">
                    @if (User.Identity.IsAuthenticated)
                    {
                        <a asp-page="/Logout" class="button is-dark">
                            <strong>Log Out</strong>
                        </a>
                    }
                    else
                    {
                        <a asp-page="/Login" class="button is-dark">
                            <strong>Sign In</strong>
                        </a>
                    }

                </div>
            </div>
        </div>
    </div>
</nav>